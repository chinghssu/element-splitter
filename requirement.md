# 需求文件 — element-splitter

## 背景

`auto_ppt` 專案裡已經有一套「平面圖轉分層動畫」的 pipeline，其中跨平台去背用的是
`tools/mask-sam.mjs`（Node + ONNX Runtime + MobileSAM，box 提示式分割）。這個獨立小工具
不是要取代那條 pipeline，而是提供一個**互動式的 GUI**，讓人可以：

- 手動畫框試 SAM 切某個元素切得準不準
- 快速把一張平面圖（插畫、商品合照、截圖）裡的元素一個個切成透明 PNG，供其他地方使用

跟 auto_ppt 完全獨立：不同語言（Python）、不同 repo、不共用程式碼。可以之後單獨分享
給其他人用，不綁 Node/monorepo。

## 使用者

單機使用，操作者本人（非工程背景的影音同事也可能用）。不需要多人協作、不需要伺服器部署。

## 目標（Must have）

1. **載入圖片**：開啟本機一張圖片（PNG/JPG）。
2. **畫框選取元素**：在圖片上用滑鼠拖曳畫一個矩形框，標出想切出的元素範圍。
3. **執行 SAM 分割**：以畫的框當 box prompt，呼叫 SAM（MobileSAM 或官方 SAM，Python 套件）
   算出該元素的分割遮罩。
4. **即時預覽結果**：顯示切出來的元素（去背後疊在棋盤格底上），可以看到遮罩準不準。
5. **多框聯集**：同一個元素可以畫多個框（例如主體 + 凸出的細長部位），結果做遮罩聯集
   ——這是 auto_ppt 那邊「咖啡杯吸管被漏切」問題的已知解法，這個工具要能重現。
6. **匯出**：把目前預覽的切出結果存成透明背景 PNG，可指定檔名/路徑。
7. **同一張圖切多個元素**：切完一個之後可以在同一張圖上繼續畫下一個元素、繼續切、繼續存，
   不必重新載入圖片（圖片的 SAM embedding 只算一次）。

## 非目標（Out of scope，這版不做）

- 不做批次處理多張圖片的自動化流程（那是 auto_ppt pipeline 的事）。
- 不做圖層排版、動畫、影片輸出。
- 不做去背以外的編修（如補洞、色彩校正）——那些留給 auto_ppt 的 `build.sh` 之類後製腳本。
- 不做雲端/多人協作、不需要帳號登入。
- 不支援點選（point prompt）模式，第一版只做 box prompt（跟現有 auto_ppt 經驗一致：
  有 bbox 提示才會準，全自動/點選容易漏切或誤併）。

## 驗收標準

- 能在 macOS 上以 `python` 指令啟動 GUI，不需要額外裝任何非 pip 可裝的東西（ffmpeg 等
  外部工具若必要，需在文件寫清楚安裝方式）。
- 對一張有 3~5 個明顯元素的測試圖，能個別畫框切出每個元素並存成獨立透明 PNG，
  目視遮罩邊緣乾淨、沒有明顯誤切/漏切。
- 換第二張圖不需要重啟程式。
